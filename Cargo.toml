[package]
name = "bcsh"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.30", features = ["process", "signal", "term", "fs"]}
glob = "0.3"
crossterm = "0.29"
signal-hook = "0.3"
libc = "0.2"
